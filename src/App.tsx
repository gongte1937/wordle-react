import { use, useEffect } from "react";
import "./App.css";

function App() {
  // can be set using api call in the future
  const solution = "hello";

  useEffect(() => {}, []);

  return <div>Hello world!</div>;
}

export default App;
